from core.trading_engine import TradingEngine

def run_grid_strategy(symbol):
    engine = TradingEngine()
    # Implémentation de la logique de grid trading
    # Ex: placement d'ordres à intervalles réguliers
    print(f"Exécution de la stratégie grid sur {symbol}")